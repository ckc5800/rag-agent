langgraph>=0.2
langchain>=0.3
langchain-ollama>=0.2
langchain-community>=0.3
langchain-text-splitters>=0.3
faiss-cpu>=1.8
# 비마크다운 코퍼스 포맷(loaders.py) — PDF/DOCX/HWPX 텍스트 추출 전용,
# 전부 순수 파이썬이라 서버·모델 다운로드가 없다.
pypdf>=4.0
python-docx>=1.1
python-hwpx>=6.0
rank_bm25>=0.2
# 벡터 저장소 대안 — VECTOR_STORE=qdrant 로 교체 (서버 없이 임베디드 모드)
langchain-qdrant>=0.2
qdrant-client>=1.12
fastapi>=0.115
uvicorn>=0.30
# mcp 2.0은 mcp.shared.context.RequestContext를 옮겨서 langchain-mcp-adapters
# 0.3.x가 임포트조차 못 한다 (ImportError: cannot import name 'RequestContext').
# 상한 없이 두면 `pip install -r requirements.txt` 한 번에 mcp 2.0.0이 잡혀
# mcp_agent.py가 죽는다 — 상한을 명시한다.
mcp>=1.0,<2
langchain-mcp-adapters>=0.1
# Graph RAG 실험(kg.py) — 순수 파이썬, 서버 불필요. FAISS·chunks.jsonl과
# 같은 "파일 하나로 끝난다" 원칙에 맞아 골랐다(cross-encoder를 rerank에서
# 안 쓴 것과 같은 이유).
networkx>=3.0
# Graph RAG 2차 검증(klue_re.py) — 공개 벤치마크(KLUE-RE) 로드 전용.
# klue_re.py 안에서만 지연 임포트해 이 프로젝트를 쓰는 다른 코드가 이
# 의존성을 강제로 물지 않는다.
datasets>=3.0
